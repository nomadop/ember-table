import { moduleFor, test } from 'ember-qunit';

moduleFor('view:grouping-column-cell', 'Unit | View | grouping column cell');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
